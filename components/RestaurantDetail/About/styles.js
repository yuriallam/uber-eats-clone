import { StyleSheet } from "react-native"


const styles = StyleSheet.create({
    restImg: {
        width: '100%',
        height: 200,
    },
    restTitle: {
        fontWeight: "600",
        fontSize: 29,
        marginHorizontal: 15,
        marginTop: 10
    },
    restDesc: {
        marginHorizontal: 15,
        marginTop: 10,
        fontWeight: '400',
        fontSize: 15.5
    }
})

export default styles;